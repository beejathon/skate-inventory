<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body class="container">
    <header>
      <%- include('../partials/header') %>
    </header>

    <main>
      <div class="row">
        <div class="col-sm-10">
          <div class="jumbotron">
            <h1><%= title %></h1>
            <p>Total items: <%= data.item_count %></p>
            <p>Decks: <%= data.deck_count %></p>
            <p>Trucks: <%= data.trucks_count %></p>
            <p>Wheels: <%= data.wheels_count %></p>
            <p>Bearings: <%= data.bearings_count %></p>
            <p>Griptape: <%= data.griptape_count %></p>
            <p>Brands: <%= data.brand_count %></p>
            <p>Categories: <%= data.category_count %></p>
          </div>
        </div>

        <%- include('../partials/sidebar') %>
      </div>
      
    </main>

    <footer>
      <%- include('../partials/footer') %>
    </footer>
  </body>
</html>
